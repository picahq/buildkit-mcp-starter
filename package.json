{
  "name": "buildkit-starter-mcp-server",
  "version": "1.0.0",
  "description": "A basic MCP (Model Context Protocol) server with one simple tool",
  "main": "build/server.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node build/server.js",
    "dev": "tsc && node build/server.js",
    "clean": "rm -rf build"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "server",
    "typescript"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.5",
    "dotenv": "^17.2.2",
    "express": "^4.18.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}